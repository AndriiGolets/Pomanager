import { PropertyUpdateEvent } from './property-update-event';

describe('PropertyUpdateEvent', () => {
  it('should create an instance', () => {
    expect(new PropertyUpdateEvent()).toBeTruthy();
  });
});
