import { MapKeyValuePipe } from './map-key-value.pipe';

describe('MapKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MapKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
